[project]
name = "muster-pattern-library"
version = "0.1.0"
description = "Machine-checkable catalog of Muster operational patterns."
requires-python = ">=3.11"
dependencies = []

[tool.muster]
manifest_schema = "schemas/pattern.schema.json"
